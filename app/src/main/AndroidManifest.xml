<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anuki.book_lovers_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity android:name=".ui.SignUpActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.ProfileActivity" />
        <activity android:name=".ui.MenuActivity" />
        <activity android:name=".ui.BooksMenuActivity" />
        <activity android:name=".ui.ComicsMenuActivity" />
        <activity android:name=".ui.ListBookActivity" />
        <activity android:name=".ui.BookDetailsActivity" />
        <activity android:name=".ui.CreateBookActivity" />
        <activity android:name=".ui.ListCommentsBooksActivity" />
        <activity android:name=".ui.CommentDetailActivity" />
        <activity android:name=".ui.ListComicActivity" />
        <activity android:name=".ui.ComicDetailsActivity" />
        <activity android:name=".ui.CreateComicActivity" />
        <activity android:name=".ui.ListCommentsComicsActivity" />
        <activity android:name=".ui.CreateBookCommentActivity" />
    </application>

</manifest>